import React from 'react';
import './SectorPreview.css';

const Thermostat = ({ area, urgency, issue, sol }) => {
  return (
    <div>
      Testinng thermostat
    </div>
  );
};

export default Thermostat;
